#include <bits/stdc++.h>
const double PI = acos(-1);
#de'
fine rep(i, n) for (int i = 0; i < (int)(n); i++ )
using namespace std;
int main(){
    long long n;
    cin >> n;
    vector<long long > a(n);
    rep(i,n) cin >> a[i];
    while(true){
        sort(a.begin(),a.end());
        if(a[0] == a[a.size()-1]) break;
        else{
            
        }
    }
}