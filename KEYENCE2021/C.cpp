#include <bits/stdc++.h>
const double PI = acos(-1);
#define rep(i, n) for (int i = 0; i < (int)(n); i++ )
using namespace std;
int main(){
    long long H, W, K;
    cin >> H >> W >> K;
    vector<long long > h(k), w(k), c(k);
    rep(i,k) cin >> h.at(i) >> w.at(i) >> c.at(i);
    
}